import React, { FC } from 'react'
import * as T from '../../types/WHTypes'

type CreateFormProps = {
    initial?: T.BaseProps
}

export const CreateWhItemForm: FC<CreateFormProps> = () => {
    return (
        <div>CreateWhItemForm</div>
    )
}
